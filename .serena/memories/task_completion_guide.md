# Task Completion Guide
1. **Workflow Edit**: `.github/workflows/` 下のYAMLファイルを編集。
2. **Local Validation**: 可能であれば `actionlint` などで構文チェックを行う。
3. **Push to Remote**: 変更を `develop` ブランチ等にプッシュ。
4. **Pull Request**: `main` ブランチへのプルリクエストを作成し、GitHub Actionsが正常に動作するか確認する。
